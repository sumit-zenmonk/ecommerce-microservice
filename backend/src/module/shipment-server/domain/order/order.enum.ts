export enum OrderPaymentStatusEnum {
    PENDING = 'pending',
    PAID = 'paid',
    CANCELLED = 'cancelled'
}

export enum OrderStatusEnum {
    PENDING = 'pending',
    PROCESSING = 'processing',
    PACKED = 'packed',
    DELIVERED = 'delivered'
}